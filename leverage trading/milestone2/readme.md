Document for milestone2

`smart-contract/` is the Aiken source for the leverage order book, at commit
fb6f71b of the contract repository. Read `smart-contract/DEPLOYMENT.md` first:
it records every script hash, every parameter and every transaction of the
preprod deployment, in the order it ran.
