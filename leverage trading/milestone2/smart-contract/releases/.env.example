# Preprod. Copy to `.env` and fill in — `.env` is gitignored.
NETWORK=Preprod
KUPO_ENDPOINT=
OGMIOS_ENDPOINT=
# 24 words for a wallet funded with test ADA. Never commit this.
DEPLOYER_SEED=
OUT_FOLDER=../deployments
